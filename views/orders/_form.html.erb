<%= form_with(model: order) do |form| %>
  <% if order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_ids %><br />
    <%= form.collection_select :product_ids, Product.all, :id, :name, {include_blank: false}, {:multiple => true, size: Product.all.count } %>
    
    <%= form.label :monto, style: "display: block" %>
    <%= form.number_field :monto %>

    <!--<%= form.label :cantidad, style: "display: block" %><%= form.number_field :cantidad %> -->
    
  </div>

<div>
  <%= form.label :product_ids %><br />
  <%= form.collection_select :product_ids, Product.all, :id, :name, {include_blank: false}, {:multiple => true, size: Product.all.count } %>
</div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
